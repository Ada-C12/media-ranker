<main class="container">
  <section class="spotlight">
    <h2 class="spotlight__header">
      <span class="spotlight__header--prefix">Media Spotlight</span>
      <a class="spotlight__link-to" href="/works/484">POODR: how to be a bike mechanic for Rubyists</a> by oops
    </h2>
  </section>
  <hr class="root__hr">
  <div class="container">
    <div class="row">
      <div class="col-sm">
        <section class="top-ten__list-container">
          <h2 class="top-ten__header">
            Top Movies
          </h2>
          <ul class="list-group top-ten__list">
            <% @movies.each do |movie| %>
              <li class="list-group-item">
                <h4>
                  <%= link_to movie.title, work_path(movie.id)%>
                  <small class="top-ten__creator">by oops</small>
                </h4>
                <p><%= movie.votes.length %> votes</p>
              </li>
            <% end %>
          </ul>
        </div>
        <div class="col-sm">
          <h2 class="top-ten__header">
            Top Books
          </h2>
          <ul class="list-group top-ten__list">
            <% @books.each do |book| %>
              <li class="list-group-item">
                <h4>
                  <%= link_to book.title, work_path(book.id)%>
                  <small class="top-ten__creator">by oops</small>
                </h4>
                <p><%= book.votes.length %> books</p>
              </li>
            <% end %>
          </ul>
        </div>
        <div class="col-sm">
          <h2 class="top-ten__header">
            Top Albums
          </h2>
          <ul class="list-group top-ten__list">
            <% @albums.each do |album| %>
              <li class="list-group-item">
                <h4>
                  <%= link_to album.title, work_path(album.id)%>
                  <small class="top-ten__creator">by oops</small>
                </h4>
                <p><%= album.votes.length %> albums</p>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
    <section class="top-ten__container">
      <section class="top-ten__list-container">
      </section>
      <section class="top-ten__list-container">
      </section>
    </main>
