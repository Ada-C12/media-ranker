<!DOCTYPE html>
<html>
  <head>
    <title>Media Ranker 2.0</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body> 
    <header class="app-header__header">
  
      <h1>
        <%=link_to "Dani's Media Ranker", root_path %>
        <small>Ranking books, albums and movies</small>
      </h1>

      <nav class="app-header__nav">
        <ul class="nav app-header__site-nav-container">
          <li class="nav-item app-header__nav_item">
            <%=link_to "Top media", root_path %>
          </li>
          <li class="nav-item app-header__nav_item">
            <%=link_to "All media", works_path %>
          </li>
          <li class="nav-item app-header__nav_item">
            <%=link_to "Add work", new_work_path%>
          </li>
          <li class="nav-item app-header__nav_item">
            <%=link_to "All users", users_path%>
          </li>    
          </ul>

          <ul class="nav app-header__user-nav-container">
          <li class="nav-item app-header__nav_item">
           <% if session[:user_id].nil? %>
            <a class="btn btn-primary" href="/login">Log In</a>
          <% elsif %>
            <a class="btn btn-primary">Logged in as <%=User.find_by(id: session[:user_id]).name%></a>
            <a class="btn btn-primary" data-method="post" href="/logout">Log Out</a>
          <% end %>
          </li>
        </ul> 
      </nav>
    </header>
     <section class="flash">
    <% flash.each do |name, message| %>
      <div class="<%= name %>"><%= message %></div>
    <% end %>
  </section>
    <%= yield %>
  </body>
</html>
