<%= link_to 'See All Media', works_path %>
<br>
<h1> Media Spotlight: </h1>
<% chosen = @works.sample %>
<%= link_to chosen.title, work_path(chosen) %>
(<%= chosen.category %>)
by
<%= chosen.creator %>

<h1> Top Books: </h1>
<ul>
<% chosen = @works.where(category: "book").top_ten %>
<% chosen.each do |book| %>
<li> <%= link_to book.title, work_path(book) %>
by
<%= book.creator %>
</li>
<% end %>
</ul>

<h1> Top Music Albums: </h1>
<ul>
<% chosen = @works.where(category: "album").top_ten %>
<% chosen.each do |album| %>
<li> <%= link_to album.title, work_path(album) %>
by
<%= album.creator %>
</li>
<% end %>
</ul>

<h1> Top Movies: </h1>
<ul>
<% chosen = @works.where(category: "movie").top_ten %>
<% chosen.each do |movie| %>
<li> <%= link_to movie.title, work_path(movie) %>
by
<%= movie.creator %>
</li>
<% end %>
</ul>
<br>
<%= link_to 'Home', root_path %>
