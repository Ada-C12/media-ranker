<h1> List of Users </h1>

<section>

  <h2> Username </h2>
  <ul>
    <% @users.each do |user| %>
      <li>
        <%= link_to user.name, user_path(user) %>
        <%= Vote.vote_count(user) %>
        <%= user.joined_date.strftime('%b %d, %Y')  %>
      </li>
    <% end %>
  </ul>


</section>