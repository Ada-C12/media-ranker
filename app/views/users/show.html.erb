<section>
  <h1> User Summary <% @user.name %> </h1>
  <h2> Joined site <% @user.joined_date.strftime('%b %d, %Y') %> </h2>
</section>


<section>
  <h3> Votes </h3>
  <ul>
  <% @user.votes.each do |vote| %>
    <li>
      <%= votes.work.title %>
      <%= votes.work.creator %>
      <%= votes.work.publication_year %>
      <%= votes.work.category.capitalize %>
      <%= votes.date.strftime('%b %d, %Y') %>
    </li>
  <% end %>
  </ul>
</section>