<h2>You are logged in as <%= User.find_by(id: session[:user_id]).username %></h2>

<p>Here is your voting history: <%= Vote.where(id: session[:user_id]) %></p>

<%= link_to 'Log Out', logout_path, method: :post %>