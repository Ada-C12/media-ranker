<main>
  <h2>List of Works</h2>
    <%= render partial: "index", locals: { works: @albums, header: 'Albums' } %>
    <%= render partial: "index", locals: { works: @books, header: 'Books'} %>
    <%= render partial: "index", locals: { works: @movies, header: 'Movies' } %>

  <%= link_to "View top media", root_path, class: "btn btn-secondary" %>
  <%= link_to "Add a new work", new_work_path, class: "btn btn-primary" %>
</main>
