<h1> List of Works </h1>
<table>
  


  <h2> albums </h2>
    <theader>
      <tr>
        <td>Votes</td>
        <td>Title</td>
        <td>Created By</td>
        <td>Published</td>
        <td>Upvote</td>
      </tr>
    </theader>
    <tbody>
      <% @works.each do |work| %>
        <% if work.category == "album"%>
          <tr>
            <td><%= Vote.where(work_id: work.id).count%></td>
            <td><%= link_to work.name, work_path(work.id) %></td>
            <td><%= work.creator %></td>
            <td><%= work.publication_year %></td>
            <td><%= link_to "Upvote", create_vote_path(work.id), method: :create %></td>
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>

  <table>
    <h2> books </h2>  
      <theader>
        <tr> 
          <td>Votes</td>
          <td>Title</td>
          <td>Created By</td>
          <td>Published</td>
          <td>Upvote</td>
        </tr>
      </theader>
    <tbody>
      <% @works.each do |work| %>
        <% if work.category == "book"%>
          <tr>
            <td><%= work.votes.count %></td>
            <td><%= link_to work.name, work_path(work.id) %></td>
            <td><%= work.creator %></td>
            <td><%= work.publication_year %></td>
            <td><%= link_to "Upvote", create_vote_path(work.id), method: :create %></td>
          </tr>
        <% end %> 
      <% end %>
    </tbody>
  </table>

  <table>
    <h2> movie </h2>
      <theader>
        <tr>
          <td>Votes</td>
          <td>Title</td>
          <td>Created By</td>
          <td>Published</td>
          <td>Upvote</td>
        </tr>
      </theader>  
      <tbody>
        <% @works.each do |work| %>
          <% if work.category == "movie"%>
            <tr>
              <td><%= work.votes.count %></td>
              <td><%= link_to work.name, work_path(work.id) %></td>
              <td><%= work.creator %></td>
              <td><%= work.publication_year %></td>
              <td><%= link_to "Upvote", create_vote_path(work.id), method: :create %></td>
            </tr>
          <% end %>
        <% end %>
    </tbody>
  </table>