<h1>List of Works</h1>

<section>
<h2>Albums</h2>
<table class="table">
  <thead>
    <tr>
      <th scope="col">Votes</th>
      <th scope="col">Title</th>
      <th scope="col">Created By</th>
      <th scope="col">Published</th>
      <th scope="col">Upvote</th>
    </tr>
  </thead>
  <tbody>
  <% albums = @works.sorted_works("album")%>
  <% albums.each do |album| %>
    <tr>
      <td><%= album.votes.length   %></td>
      <td><%= link_to album.title, work_path(album.id)   %></td>
      <td><%= album.creator  %></td>
      <td><%= album.publication_year %></td>
      <td><%= link_to "Upvote", work_votes_path(album.id), method: :post  %></td>
    </tr>
  <% end %>
  </tbody>
</table>
</section>

<section>
<h2>Books</h2>
<table class="table">
  <thead>
    <tr>
      <th scope="col">Votes</th>
      <th scope="col">Title</th>
      <th scope="col">Created By</th>
      <th scope="col">Published</th>
      <th scope="col">Upvote</th>
    </tr>
  </thead>
  <tbody>
  <% books = @works.sorted_works("book")%>
  <% books.each do |book| %>
    <tr>
      <td><%= book.votes.length   %></td>
      <td><%= link_to book.title, work_path(book.id)   %></td>
      <td><%= book.creator  %></td>
      <td><%= book.publication_year %></td>
      <td><%= link_to "Upvote", work_votes_path(book.id), method: :post  %></td>
    </tr>
  <% end %>
  </tbody>
</table>
</section>

<section>
<h2>Movies</h2>
<table class="table">
  <thead>
    <tr>
      <th scope="col">Votes</th>
      <th scope="col">Title</th>
      <th scope="col">Created By</th>
      <th scope="col">Published</th>
      <th scope="col">Upvote</th>
    </tr>
  </thead>
  <tbody>
  <% movies = @works.sorted_works("movie")%>
  <% movies.each do |movie| %>
    <tr>
      <td><%= movie.votes.length   %></td>
      <td><%= link_to movie.title, work_path(movie.id)   %></td>
      <td><%= movie.creator  %></td>
      <td><%= movie.publication_year %></td>
      <td><%= link_to "Upvote", work_votes_path(movie.id), method: :post  %></td>
    </tr>
  <% end %>
  </tbody>
</table>
</section>

<span>
<%= link_to "View top media", root_path, class: "btn btn-secondary btn space"%>
<%= link_to "Add a new work", new_work_path, class: "btn btn-primary btn space"%>
</span>