  <main>
  <h2>List of Works</h2>
  <section class="media-table">
    <h4>Albums</h4>
    <table class="table">
      <thead>
        <tr>
          <th>Votes</th>
          <th>Title</th>
          <th>Created By</th>
          <th>Published</th>
          <th>Upvote</th>
        </tr>
      </thead>
      <% Work.where(category: "album").sort_by { |work| work.votes.count }.reverse!.each do |work| %>
      <tbody>
        <td><%= work.votes.count %></td>
        <td><%= link_to work.title, work_path(work.id) %></td>
        <td><%= work.creator %></td>
        <td><%= work.publication_year %></td>
        <td><%= button_to "Upvote", work_votes_path(work.id), class: "btn btn-primary", method: :post %></td>
      <% end %>
      </tbody>
    </table>
  </section>

  <section class="media-table">
    <h4>Books</h4>
    <table class="table">
      <thead>
        <tr>
          <th>Votes</th>
          <th>Title</th>
          <th>Created By</th>
          <th>Published</th>
          <th>Upvote</th>
        </tr>
      </thead>
      <% Work.where(category: "book").sort_by { |work| work.votes.count }.reverse!.each do |work| %>
      <tbody>
        <td><%= work.votes.count %></td>
        <td><%= link_to work.title, work_path(work.id) %></td>
        <td><%= work.creator %></td>
        <td><%= work.publication_year %></td>
        <td><%= button_to "Upvote", work_votes_path(work.id), class: "btn btn-primary", method: :post %></td>
      <% end %>
      </tbody>
    </table>
  </section>

  <section class="media-table">
    <h4>Movies</h4>
    <table class="table">
      <thead>
        <tr>
          <th>Votes</th>
          <th>Title</th>
          <th>Created By</th>
          <th>Published</th>
          <th>Upvote</th>
        </tr>
      </thead>
      <% Work.where(category: "movie").sort_by { |work| work.votes.count }.reverse!.each do |work| %>
      <tbody>
        <td><%= work.votes.count %></td>
        <td><%= link_to work.title, work_path(work.id) %></td>
        <td><%= work.creator %></td>
        <td><%= work.publication_year %></td>
        <td><%= button_to "Upvote", work_votes_path(work.id), class: "btn btn-primary", method: :post %></td>
      <% end %>
      </tbody>
    </table>
  </section>
</main>
