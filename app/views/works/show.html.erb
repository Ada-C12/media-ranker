
<h2><%= @work.title%></h2>
<h3><%= @work.creator%>

<h3><%= @work.publication_year%></h3>
<h3><%= @work.description%></h3>

<%= link_to "Back to media", root_path(@work) %> 
<%= link_to "Edit", edit_work_path(@work) %> 
<%= link_to "upvote", edit_work_path(@work) %> 
<%= link_to "Delete", work_path(@work), method: :delete, data: {confirm: "Are you sure?"}%>
