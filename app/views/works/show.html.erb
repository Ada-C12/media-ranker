<section>
  <h2><%= @work.title %></h2>

  <p>Created by: <%= @work.creator %></p>
  <p>Published: <%= @work.publication_year %></p>

  <section>
    <%= link_to "Back to media ranks", root_path, method: :get%>
    <%= link_to "Edit", edit_work_path, method: :get%>
    <%= link_to "Upvote" %>
    <%= link_to "Delete", work_path(@work.id), method: :delete%>
  </section>
</section>

<section>
  <h3>NUMBER votes for this CATEGORY</h3>
</section>