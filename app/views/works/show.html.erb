<section>
  <p><%= @work.category %></p>
  <p><%= @work.title %></p>
  <p><%= @work.creator %></p>
  <p><%= @work.publication_year %></p>
  <p><%= @work.description %></p>

  <%= link_to "Edit", edit_work_path(@work)  %> | <%= link_to "Delete", work_path(@work), method: :delete, data: {confirm: "Are you sure you would like to delete this work?"} %>

</section>