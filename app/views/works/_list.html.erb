<section class="media-table">
  <h4><%= (works)&.first&.category&.to_s&.capitalize&.+('s') %></h4>
  <table class="table">
    <thead>
      <tr>
        <th width="80">Votes</th>
        <th>Title</th>
        <th>Created By</th>
        <th width="100">Published</th>
        <th width="100">Upvote</th>
      </tr>
    </thead>
    <tbody>
      <% (works).each do |w| %>
        <tr>
          <td><%= %></td>
          <td><%= link_to w.title, work_path(w.id) %></td>
          <td><%= w.creator %></td>
          <td><%= w.published %></td>
          <td><%= %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</section>
